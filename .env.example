# Copy this file to `.env` in the same directory as `requirements.txt` (optional).
# Variables here are loaded on startup. If set, ANTHROPIC_API_KEY overrides
# any key stored in config.json for runtime API calls (Settings still edits config.json).

ANTHROPIC_API_KEY=
